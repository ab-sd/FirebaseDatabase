name: Android CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build (assembleDebug + unit tests, but delete APK)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print environment (debug)
        run: |
          echo "Runner: $RUNNER_OS"
          java -version || true
          ./gradlew --version || true

      - name: Set up JDK 17 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK (commandline tools + accept licenses)
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages (platforms + build-tools)
        run: |
          yes | sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"
          yes | sdkmanager "cmdline-tools;latest"

      - name: Inject google-services.json (decoded from secret if provided)
        env:
          GS_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -n "$GS_JSON_BASE64" ]; then
            mkdir -p app
            echo "$GS_JSON_BASE64" | base64 --decode > app/google-services.json
            echo "Wrote app/google-services.json (decoded from secret)"
          else
            echo "No GOOGLE_SERVICES_JSON secret provided — skipping creation of app/google-services.json"
          fi

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*','**/gradle-wrapper.properties','**/settings.gradle*','**/settings.gradle.kts','**/gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run unit tests (if any)
        run: ./gradlew testDebugUnitTest --no-daemon --stacktrace

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Delete generated APKs to free space
        run: |
          echo "Deleting generated APK files..."
          rm -f app/build/outputs/apk/**/*.apk || true
          rm -rf app/build/outputs || true
